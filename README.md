# Fat2Fit - نظام إدارة النادي النسائي

نظام إدارة شامل للنادي النسائي يتضمن إدارة العميلات، الاشتراكات، المدفوعات، الفواتير، وتسجيل الدخول.

## المميزات

### 🎯 الوظائف الرئيسية

1. **إدارة العميلات**
   - تسجيل عميلات جديدة
   - ملف شخصي كامل لكل عميلة
   - سجل شامل للاشتراكات والمدفوعات

2. **إدارة الاشتراكات**
   - باقات متنوعة (شهري، ربع سنوي، سنوي، VIP، حصص)
   - تجديد وتحديث الاشتراكات
   - تجميد الاشتراكات
   - تنبيهات قرب انتهاء الاشتراك

3. **نظام الدفع والفواتير**
   - فواتير إلكترونية
   - طرق دفع متعددة (كاش، مدى، Apple Pay، STC Pay، بطاقات)
   - تتبع المديونيات
   - سجل مدفوعات كامل

4. **تسجيل الدخول والحضور**
   - تسجيل دخول سريع
   - البحث بالاسم، رقم الجوال، أو رقم العضوية
   - منع الدخول للاشتراكات المنتهية

5. **التقارير**
   - تقارير يومية وشهرية
   - إحصائيات المبيعات
   - تتبع الأداء

## التكنولوجيا المستخدمة

- **Next.js 16** - إطار عمل React
- **TypeScript** - للبرمجة الآمنة
- **Prisma** - ORM لقاعدة البيانات
- **SQLite** - قاعدة البيانات (قابلة للتبديل إلى PostgreSQL)
- **NextAuth** - نظام المصادقة
- **Tailwind CSS** - للتصميم
- **Zod** - للتحقق من البيانات

## التثبيت والتشغيل

### المتطلبات

- Node.js 18 أو أحدث
- npm أو yarn

### خطوات التثبيت

1. استنساخ المشروع:
```bash
git clone <repository-url>
cd fat2fit
```

2. تثبيت المكتبات:
```bash
npm install
```

3. إعداد قاعدة البيانات:
```bash
npm run db:generate
npm run db:migrate
```

4. تهيئة البيانات الافتراضية:
```bash
npm run seed
```

سيتم إنشاء:
- مستخدم مدير: `admin@fat2fit.com` / `admin123`
- موظفة استقبال: `reception@fat2fit.com` / `reception123`
- باقات اشتراك افتراضية

5. تشغيل المشروع:
```bash
npm run dev
```

افتح المتصفح على: http://localhost:3000

## الصلاحيات

### استقبال (RECEPTION)
- تسجيل عميلات جديدة
- تجديد الاشتراكات
- تسجيل المدفوعات
- تسجيل الدخول
- عرض التقارير

### مديرة (ADMIN)
- جميع صلاحيات الاستقبال
- إدارة الباقات
- إنشاء كوبونات
- التقارير الكاملة
- إدارة المستخدمين

## البنية

```
fat2fit/
├── app/
│   ├── api/              # API Routes
│   ├── dashboard/        # صفحات لوحة التحكم
│   ├── login/           # صفحة تسجيل الدخول
│   └── layout.tsx       # Layout الرئيسي
├── components/          # المكونات المشتركة
├── lib/                 # المكتبات والمساعدات
├── prisma/
│   └── schema.prisma    # مخطط قاعدة البيانات
└── scripts/
    └── seed.ts          # سكريبت التهيئة
```

## الأوامر المتاحة

```bash
npm run dev          # تشغيل المشروع في وضع التطوير
npm run build        # بناء المشروع للإنتاج
npm run start        # تشغيل المشروع بعد البناء
npm run lint         # فحص الكود
npm run seed         # تهيئة البيانات الافتراضية
npm run db:generate  # توليد Prisma Client
npm run db:migrate   # تطبيق التغييرات على قاعدة البيانات
npm run db:studio    # فتح Prisma Studio لإدارة قاعدة البيانات
```

## ملاحظات مهمة

1. **الأمان**: قم بتغيير `NEXTAUTH_SECRET` في ملف `.env` في بيئة الإنتاج
2. **قاعدة البيانات**: يمكنك تبديل SQLite بـ PostgreSQL أو MySQL للإنتاج
3. **الصور**: نظام رفع الصور لم يتم تنفيذه بعد (يمكن إضافته لاحقاً)

## التطوير المستقبلي

- [ ] نظام إشعارات SMS/واتساب
- [ ] تصدير التقارير لـ Excel
- [ ] نظام الجلسات الخاصة
- [ ] تقارير الصحة واللياقة
- [ ] نظام الموظفات والمدربات
- [ ] نظام الحجوزات

## الدعم

للأسئلة والدعم، يرجى التواصل مع فريق التطوير.

## الترخيص

جميع الحقوق محفوظة © 2025
