# ملخص التعديلات على نظام النادي

## ✅ التعديلات المنجزة

### 1. إضافة رقم الهوية للعضو/العضوة ✅
- ✅ إضافة حقل `idNumber` في `Client` model (Prisma Schema)
- ✅ إضافة حقل رقم الهوية في نموذج إنشاء العضوية
- ✅ تحديث API route لدعم رقم الهوية

### 2. تحديث المصطلحات ✅
- ✅ تغيير "العميلات" إلى "العضوية" في Sidebar
- ✅ تغيير "تسجيل عميلة جديدة" إلى "إنشاء عضوية جديدة"
- ✅ تحديث وصف الصفحة

### 3. إضافة نوع الاشتراك في الفاتورة ✅
- ✅ إضافة حقل `subscriptionType` في `Invoice` model
- ✅ تحديث API route لاستخراج نوع الاشتراك من الباقة وحفظه في الفاتورة

---

## 🔄 التعديلات المتبقية (تتطلب تطوير إضافي)

### 4. تحسين التقارير
#### 4.1 تقرير الاشتراكات (النشطة، المقبولة، الموقفة)
- [ ] إضافة فلتر حسب حالة الاشتراك في صفحة التقارير
- [ ] إضافة إحصائيات منفصلة لكل حالة
- [ ] عرض الاشتراكات الموقفة مع تاريخ التجميد

#### 4.2 تقرير المبيعات
- [ ] إضافة قسم للأعضاء الجدد (تم إنشاء العضوية في الفترة المحددة)
- [ ] إضافة قسم للتجديد (تم تجديد الاشتراك في الفترة المحددة)
- [ ] إحصائيات المبيعات (عدد، إيرادات)

#### 4.3 تقرير الأعضاء
- [ ] إضافة تقرير جديد يعرض:
  - نوع الاشتراك (الباقة)
  - مدة الاشتراك
  - حالة الاشتراك (نشط، منتهي، موقوف)
  - عدد الأعضاء لكل نوع/حالة

#### 4.4 تحسين التقرير المالي
- [ ] إضافة البحث/الفلترة حسب المدة (تاريخ من - إلى)
- [ ] تحسين عرض الإحصائيات المالية

### 5. تحسينات إضافية
- [ ] إضافة صفحة الشروط والأحكام
- [ ] تحسين عرض الخدمات (التدريب الشخصي وغيره) في الداشبورد
- [ ] تحديث باقي المصطلحات من "عميلة" إلى "عضوية" في جميع الصفحات

---

## 📝 ملاحظات للتنفيذ

### Migration مطلوب
بعد إضافة الحقول الجديدة (`idNumber` في Client، `subscriptionType` في Invoice)، يجب تشغيل:

```bash
npx prisma migrate dev --name add_id_number_and_subscription_type
```

أو على Render:
```bash
npx prisma migrate deploy
```

### الملفات المعدلة
1. `prisma/schema.prisma` - إضافة `idNumber` و `subscriptionType`
2. `app/dashboard/clients/new/page.tsx` - إضافة حقل رقم الهوية
3. `app/dashboard/layout.tsx` - تحديث المصطلحات
4. `app/api/clients/route.ts` - دعم `idNumber`
5. `app/api/invoices/route.ts` - إضافة `subscriptionType`

---

## 🚀 الخطوات التالية

1. **تشغيل Migration** لتطبيق تغييرات قاعدة البيانات
2. **تحديث باقي المصطلحات** في جميع الملفات (استخدام grep للبحث)
3. **تطوير التقارير** حسب المتطلبات المتبقية
4. **اختبار النظام** للتأكد من عمل جميع التغييرات

---

✅ **الجزء الأساسي من التعديلات جاهز!**

