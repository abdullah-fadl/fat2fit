// Temporary schema for local SQLite development
// Copy this to schema.prisma for local development

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Rest of schema stays the same...
// (Copy the rest from schema.prisma)

