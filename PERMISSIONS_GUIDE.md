# 🔐 دليل الصلاحيات في النظام

## الأدوار المتوفرة:

### 1. ADMIN (مديرة)
- ✅ **كل الصلاحيات** - الوصول الكامل لجميع الميزات
- يمكنها الوصول إلى:
  - جميع الصفحات والوظائف
  - أجهزة البصمة (ZK Devices)
  - سجل الأنشطة (Audit Log)
  - إدارة الموظفات
  - حذف الفواتير والبيانات

### 2. RECEPTION (استقبال)
- ✅ الوصول إلى الميزات الأساسية:
  - إدارة العميلات (عرض، إنشاء، تعديل)
  - إدارة الاشتراكات (عرض، إنشاء، تجديد، تجميد)
  - عرض الباقات
  - عرض الكوبونات
  - إدارة الفواتير والمدفوعات
  - تسجيل الدخول
  - الحجوزات
  - التقارير

- ❌ **لا يمكنها الوصول إلى:**
  - أجهزة البصمة (ZK Devices)
  - سجل الأنشطة (Audit Log)
  - إدارة الموظفات (Staff Management)
  - حذف العميلات
  - حذف الفواتير

### 3. TRAINER (مدربة)
- ✅ الوصول المحدود:
  - عرض العميلات فقط
  - الحجوزات
  - التقييم الصحي

- ❌ **لا يمكنها الوصول إلى:**
  - الاشتراكات
  - الفواتير
  - الباقات
  - الكوبونات
  - أجهزة البصمة
  - سجل الأنشطة
  - الموظفات
  - التقارير

---

## الصلاحيات التفصيلية:

### العميلات (Clients)
- `clients.view` - عرض قائمة العميلات
- `clients.create` - إضافة عميلة جديدة
- `clients.edit` - تعديل بيانات العميلة
- `clients.delete` - حذف العميلة (ADMIN فقط)

### الاشتراكات (Subscriptions)
- `subscriptions.view` - عرض الاشتراكات
- `subscriptions.create` - إنشاء اشتراك جديد
- `subscriptions.renew` - تجديد الاشتراك
- `subscriptions.freeze` - تجميد الاشتراك
- `subscriptions.cancel` - إلغاء الاشتراك

### الباقات (Packages)
- `packages.view` - عرض الباقات
- `packages.create` - إنشاء باقة جديدة
- `packages.edit` - تعديل الباقة
- `packages.delete` - حذف الباقة

### الكوبونات (Coupons)
- `coupons.view` - عرض الكوبونات
- `coupons.create` - إنشاء كوبون جديد
- `coupons.edit` - تعديل الكوبون
- `coupons.delete` - حذف الكوبون

### الفواتير (Invoices)
- `invoices.view` - عرض الفواتير
- `invoices.create` - إنشاء فاتورة جديدة
- `invoices.edit` - تعديل الفاتورة
- `invoices.delete` - حذف الفاتورة (ADMIN فقط)

### المدفوعات (Payments)
- `payments.create` - تسجيل دفعة جديدة

### تسجيل الدخول (Check-in)
- `checkin.access` - تسجيل دخول العميلات

### الموظفات (Staff)
- `staff.view` - عرض الموظفات
- `staff.create` - إضافة موظفة جديدة
- `staff.edit` - تعديل بيانات الموظفة
- `staff.delete` - حذف الموظفة (ADMIN فقط)

### أجهزة البصمة (ZK Devices) - **جديد** 🔒
- `zk_devices.view` - عرض أجهزة البصمة
- `zk_devices.manage` - إدارة أجهزة البصمة (إنشاء، تعديل، حذف، تسجيل بصمة، مزامنة)

**الوصول:** ADMIN فقط

### سجل الأنشطة (Audit Log) - **محمي** 🔒
- `audit_log.view` - عرض سجل الأنشطة

**الوصول:** ADMIN فقط

---

## كيفية إضافة صلاحيات جديدة:

### 1. إضافة الصلاحية في `lib/permissions.ts`:
```typescript
export const PERMISSIONS = {
  // ... الصلاحيات الموجودة
  NEW_FEATURE_VIEW: "new_feature.view",
  NEW_FEATURE_MANAGE: "new_feature.manage",
}
```

### 2. إضافة الصلاحية للأدوار في `ROLE_PERMISSIONS`:
```typescript
RECEPTION: [
  // ... الصلاحيات الموجودة
  PERMISSIONS.NEW_FEATURE_VIEW, // للاستقبال
],
```

### 3. استخدام الصلاحية في API Routes:
```typescript
import { requirePermission } from "@/lib/auth-utils"
import { PERMISSIONS } from "@/lib/permissions"

const permCheck = await requirePermission(PERMISSIONS.NEW_FEATURE_VIEW)
if (permCheck.error) {
  return permCheck.response
}
```

### 4. استخدام الصلاحية في الصفحات (Sidebar):
```typescript
{
  name: "الميزة الجديدة",
  href: "/dashboard/new-feature",
  icon: Icon,
  permission: PERMISSIONS.NEW_FEATURE_VIEW,
}
```

---

## ملاحظات مهمة:

1. **الأمان:**
   - الصلاحيات يتم التحقق منها في:
     - API Routes (server-side)
     - Sidebar Navigation (client-side)
   - يجب دائماً التحقق من الصلاحيات في API Routes لضمان الأمان

2. **ADMIN لديه كل الصلاحيات:**
   - `ADMIN` role يحصل تلقائياً على جميع الصلاحيات
   - لا حاجة لإضافة صلاحيات محددة له

3. **تخصيص الصلاحيات:**
   - يمكنك تعديل `ROLE_PERMISSIONS` لإضافة أو إزالة صلاحيات لأي دور
   - تأكد من تحديث API Routes والواجهات عند إضافة صلاحيات جديدة

---

✅ **تم تحديث النظام بحيث يمكن التحكم الكامل في الصلاحيات!**


