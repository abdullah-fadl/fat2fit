# 🔒 دليل الأمان والخصوصية

## الميزات المتوفرة:

### 1. التشفير ✅
- تشفير البيانات الحساسة (أرقام الهواتف، الأسماء، البريد الإلكتروني)
- تشفير AES-256-GCM
- حماية من الوصول غير المصرح للبيانات

### 2. النسخ الاحتياطي التلقائي ✅
- نسخ احتياطي تلقائي من قاعدة البيانات
- جدولة النسخ (كل X ساعة)
- الاحتفاظ بالنسخ لعدد محدد من الأيام
- تنظيف تلقائي للنسخ القديمة

### 3. تسجيل الوصول والأنشطة ✅
- تسجيل جميع محاولات تسجيل الدخول (نجاح/فشل)
- تسجيل الوصول للبيانات الحساسة
- تسجيل تصدير البيانات
- تسجيل الوصول للنسخ الاحتياطية

### 4. إعدادات الأمان ✅
- كلمات مرور قوية
- انتهاء الجلسة بعد مدة محددة
- حد أقصى لمحاولات تسجيل الدخول
- إخفاء البيانات الحساسة في السجلات

### 5. الالتزام بالخصوصية (GDPR) ✅
- حق حذف البيانات
- فترة احتفاظ محددة بالبيانات
- حماية بيانات العضوات
- سياسة خصوصية واضحة

---

## كيفية الاستخدام:

### 1. إعداد التشفير:

1. أضف مفتاح التشفير إلى `.env`:
```env
ENCRYPTION_KEY=your-32-or-64-character-encryption-key-here
```

يمكن توليد مفتاح باستخدام:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

2. فعّل التشفير من صفحة **الأمان والخصوصية**:
   - تفعيل التشفير
   - تفعيل تشفير أرقام الهواتف
   - تفعيل تشفير الأسماء (اختياري)
   - تفعيل تشفير البريد الإلكتروني (اختياري)

### 2. إعداد النسخ الاحتياطي التلقائي:

#### يدوياً:
```bash
POST /api/backup/create
```

#### تلقائياً (Cron Job):
```bash
# كل يوم الساعة 2 صباحاً
0 2 * * * curl -X POST https://your-domain.com/api/backup/create -H "Authorization: Bearer YOUR_TOKEN"
```

أو باستخدام Node.js:
```javascript
const cron = require('node-cron');

cron.schedule('0 2 * * *', async () => {
  await fetch('https://your-domain.com/api/backup/create', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.ADMIN_TOKEN}`
    }
  });
});
```

#### عرض قائمة النسخ:
```bash
GET /api/backup/list
```

### 3. مراقبة الوصول:

- **سجل الوصول**: `/api/access-logs`
- **سجل الأنشطة**: `/dashboard/audit-logs`
- **الأمان والخصوصية**: `/dashboard/security`

---

## إعدادات الأمان المتقدمة:

### 1. كلمات المرور القوية:
- الحد الأدنى 8 أحرف
- يجب أن تحتوي على حروف كبيرة وصغيرة
- يجب أن تحتوي على أرقام
- يجب أن تحتوي على رموز خاصة

### 2. انتهاء الجلسة:
- افتراضي: 60 دقيقة
- يمكن التخصيص من صفحة الأمان

### 3. حد محاولات تسجيل الدخول:
- افتراضي: 5 محاولات
- بعد تجاوز الحد: يتم حظر IP مؤقتاً

---

## الالتزام بالخصوصية:

### 1. حماية بيانات العضوات:
- ✅ التشفير للبيانات الحساسة
- ✅ صلاحيات محددة للوصول
- ✅ تسجيل جميع الوصولات
- ✅ حذف البيانات عند الطلب (GDPR)

### 2. حق حذف البيانات:
- يمكن للعضوات طلب حذف بياناتهن
- يتم حذف جميع البيانات المرتبطة
- يتم الحفاظ على السجلات المالية (للمحاسبة)

### 3. فترة الاحتفاظ:
- يمكن تحديد فترة احتفاظ بالبيانات
- بعد انتهاء الفترة: يتم حذف البيانات تلقائياً
- السجلات المالية: محفوظة دائماً (حسب القانون)

---

## API Endpoints:

### النسخ الاحتياطي:
- `POST /api/backup/create` - إنشاء نسخة احتياطية
- `GET /api/backup/list` - قائمة النسخ

### إعدادات الأمان:
- `GET /api/security/settings` - الحصول على الإعدادات
- `PUT /api/security/settings` - تحديث الإعدادات

### سجل الوصول:
- `GET /api/access-logs` - سجل الوصول والأنشطة

---

## الصلاحيات المطلوبة:

### إدارة الأمان:
- **ADMIN فقط** - الوصول الكامل
- **RECEPTION** - لا يمكنها الوصول

---

## متغيرات البيئة المطلوبة:

```env
# مفتاح التشفير (32 أو 64 حرف)
ENCRYPTION_KEY=your-encryption-key-here

# API Key للعمليات الداخلية
INTERNAL_API_KEY=your-internal-api-key-here
```

---

## ملاحظات مهمة:

1. **مفتاح التشفير**: احفظه بشكل آمن ولا تشاركه
2. **النسخ الاحتياطية**: احفظها في مكان آمن منفصل
3. **الصلاحيات**: راجع الصلاحيات بانتظام
4. **السجلات**: راجع سجلات الوصول بانتظام
5. **GDPR**: تأكد من الالتزام بقوانين حماية البيانات

---

## سياسة الخصوصية:

النظام يتضمن:
- ✅ تشفير البيانات الحساسة
- ✅ حماية من الوصول غير المصرح
- ✅ تسجيل جميع الأنشطة
- ✅ حق حذف البيانات
- ✅ فترة احتفاظ محددة

**مهم جداً للأندية النسائية**: جميع البيانات محمية ومشفرة.

---

✅ **النظام جاهز لحماية بيانات العضوات والالتزام بالخصوصية!**


